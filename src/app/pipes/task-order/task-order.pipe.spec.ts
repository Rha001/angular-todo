import { TaskOrderPipe } from './task-order.pipe';

describe('TaskOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
